#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char *user_input = NULL;
  size_t len = 0;
  getline(&user_input, &len, stdin);
  printf("%s", user_input);

  free(user_input);
}
